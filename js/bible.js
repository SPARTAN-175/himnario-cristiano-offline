import{all,put,bulk}from"./db.js";export const versions=()=>all("bibleVersions");export const books=()=>all("bibleBooks");export async function importBible(x){if(!x?.version?.id||!Array.isArray(x.books))throw Error("Biblia inválida");await put("bibleVersions",x.version);const bs=[],vs=[];for(const b of x.books){bs.push({id:x.version.id+":"+b.id,versionId:x.version.id,name:b.name,order:b.order,chapters:b.chapters||[]});for(const c of b.chapters||[])for(const v of c.verses||[])vs.push({id:`${x.version.id}:${b.id}:${c.number}:${v.number}`,versionId:x.version.id,bookId:b.id,chapter:c.number,number:v.number,text:v.text})}await bulk("bibleBooks",bs);await bulk("bibleVerses",vs)}export async function chapter(v,b,c){return(await all("bibleVerses")).filter(x=>x.versionId===v&&x.bookId===b&&x.chapter===c).sort((a,b)=>a.number-b.number)}export const notes=r=>all("notes").then(a=>a.filter(x=>x.ref===r));export const note=n=>put("notes",n)