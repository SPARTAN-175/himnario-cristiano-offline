import{go}from"../js/router.js";import{list,fav}from"../js/hymns.js";import{shell,nav,esc,toast}from"../js/ui.js";
export async function hymns(){shell("Himnos","hymns");const v=document.getElementById("view"),d=await list();v.innerHTML=`<div class="search"><input id="q" placeholder="Número, título o tema..."></div><div class="actions filter-actions"><button class="btn primary" id="all">Todos</button><button class="btn" id="onlyFav">★ Favoritos</button></div><section id="l" class="grid"></section>`;let favoritesOnly=false;const render=q=>{const a=d.filter(x=>(!favoritesOnly||x.favorite)&&(x.title+" "+x.number+" "+x.theme).toLowerCase().includes(q.toLowerCase()));document.getElementById("l").innerHTML=a.map(x=>`<div class="card click"><div class="iconbox">${x.number}</div><div class="grow" data-h="${x.id}"><div class="title">${esc(x.title)}</div><div class="muted">${esc(x.theme)}</div></div><button class="btn" data-f="${x.id}" aria-label="Favorito">${x.favorite?"★":"☆"}</button></div>`).join("")||`<div class="empty">${favoritesOnly?"No tienes himnos favoritos.":"Sin resultados."}</div>`;document.querySelectorAll("[data-h]").forEach(b=>b.onclick=()=>go("hymn",b.dataset.h));document.querySelectorAll("[data-f]").forEach(b=>b.onclick=async e=>{e.stopPropagation();await fav(b.dataset.f);const item=d.find(x=>x.id===b.dataset.f);if(item)item.favorite=!item.favorite;toast("Favorito actualizado");render(q)})};document.getElementById("q").oninput=e=>render(e.target.value);document.getElementById("all").onclick=()=>{favoritesOnly=false;document.getElementById("all").classList.add("primary");document.getElementById("onlyFav").classList.remove("primary");render(document.getElementById("q").value)};document.getElementById("onlyFav").onclick=()=>{favoritesOnly=true;document.getElementById("onlyFav").classList.add("primary");document.getElementById("all").classList.remove("primary");render(document.getElementById("q").value)};nav(go);render("")}
